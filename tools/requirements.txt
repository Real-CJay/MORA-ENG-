# Offline extraction tools only; the static website has no Python dependency.
# The crop window uses tkinter from the standard library, so OpenCV and numpy are
# no longer required. OpenCV ships an unsigned native DLL that Windows Smart App
# Control blocks, which made the cropper unusable on locked-down machines.
PyMuPDF==1.28.2
Pillow==12.3.0
